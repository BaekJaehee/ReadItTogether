version: '3.8'
services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
       - /etc/letsencrypt:/etc/letsencrypt:ro
       - /home/dudtjr20027/default.conf:/etc/nginx/conf.d/default.conf:ro
